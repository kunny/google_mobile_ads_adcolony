name: google_mobile_ads_adcolony
description: Google Mobile Ads mediation plugin for AdColony.
version: 2.5.0
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.androidhuman.plugins.google_mobile_ads_adcolony
        pluginClass: GoogleMobileAdsAdcolonyPlugin
      ios:
        pluginClass: AHMGoogleMobileAdsAdcolonyPlugin
